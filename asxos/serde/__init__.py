"""Serialisation primitives shared across the domain layer.

Leaf package: modules here import stdlib only, never ``asxos.domain.*`` or
``asxos.brief.*``. Anything may import from here; nothing here imports back.
"""
