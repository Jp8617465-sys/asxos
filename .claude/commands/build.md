# build — one-file reversible path — `/build`

`$ARGUMENTS` = a file path plus the intent. No default.

**This is the fast speed.** No Guilfoyle graph. No red-team. Multi-node work is
`/arbi-mission`. Operating map: `docs/product/harness-profiles.md`.

Stay here only for one file, a same-file refactor, or a tiny sequential fix.
I5/I6 / P5/P6 STOP for James.

1. Look up the hard owner in `docs/product/harness-profiles.md`.
2. Edit on a `claude/**` or `cursor/**` branch.
3. Targeted pytest / ruff / mypy. Risk-tiered consult.
4. Draft PR only if needed. Never merge.
