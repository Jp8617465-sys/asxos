# arbi run — DEPRECATED — use `/arbi-mission`

`$ARGUMENTS` are ignored. Do not plan or fan out from this file.

**`/arbi-run` is retired as the live dispatch bridge.** Use `/arbi-mission`.
One-file work is `/build`. Large parallel work is `/arbi-team`.
Operating map: `docs/product/harness-profiles.md`.
