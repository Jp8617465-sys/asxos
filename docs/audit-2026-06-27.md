# asxos full product audit — verified findings register

_Generated 2026-06-27 from workflow wxtxbkx98 — 106 files fully read (every line), 168 agents, adversarially verified._

**Confirmed:** 3 high · 39 med · 40 low · 23 info.  10 findings refuted by verification, 2 uncertain.
**Post-review correction:** of the 3 HIGH, **1 is real (fixed)** and **2 were false positives** (see below).

## HIGH (1 real + 2 false-positive on review)

- ✅ **FIXED — [firewall] `asxos/cli/holdings.py`** — import_holdings() CLI entry point wrote to holding_lots but never called _require_personal_use(), bypassing the Part 0 Q1 regulatory firewall. Fixed: gate added as the first statement; verified by portfolio-invariant-guard (sole registered command; dry-run path also gated).
- ❌ **FALSE POSITIVE — [bug] `asxos/domain/brief/collectors/market_context.py:62-65`** — claimed `row.get(...)` raises AttributeError because "asyncpg.Record has no .get()". **Refuted on review:** `asyncpg==0.29.0` (pyproject L18) — `Record.get()` has existed since asyncpg 0.18, and production already relies on it (`active_theses.py:121`). No crash.
- ❌ **FALSE POSITIVE — [untested] `market_context.py:62-65`** — claimed the test masks the bug. Moot: the `.get()` bug is not real. `tests/test_brief_v2_sections.py:35` `_make_row` fakes **both** `.get` and `__getitem__`, faithfully mirroring a real Record.

> **Meta-finding:** both the auditor and its adversarial verifier shared a gap in asyncpg
> API knowledge, so a false positive passed verification. Adversarial verification is not
> infallible when the blind spot is shared — the MED triage below re-verifies, it does not assume.

## MED triage (dispositions)

Re-verified for the asyncpg-class blind spot — **no further false positives** among the
39 (the rest are logic/SQL/schema claims, not library-API-existence). ~18 of the 39 are
themselves coverage gaps → routed to `docs/backlog-test-coverage.md`. The ~21 logic
findings split:

**FIX-NOW (verified real, high-value, tractable — recommended next batch):**
- `tax/positions.py:64` (+ `tax/medicare.py:22` docstring) — CGT-branch Medicare ignores `config.medicare_levy_rate` (self-introduced Phase-1 regression). Tax correctness.
- `cli/thesis.py` — 9 core thesis commands ungated (firewall) — same class as the fixed HIGH.
- `domain/brief/collectors/tax_operational.py:57` — `relevance_tags` JSONB treated as dict not array → regulatory items silently dropped.
- `domain/brief/collectors/wealth_state.py:98` — concentration numerator/denominator from mismatched sources.
- `portfolio/monitor_loader.py:105` — `prob_up`/`expected_return` `_dec()` with no None-guard → NULL crashes the load.
- `models/train.py:159` — empty walk-forward → silent `nan` instead of failing loudly (non-negotiable #10).
- `signals/feature_engine.py:122` — no ordering guard; unsorted input → silently wrong momentum/vol features.
- `brief/compose.py:184` — regime query non-reproducible (no model_version filter / ORDER BY).
- `cli/tax.py:185` — `eligible_at` uses +366 days, not calendar +1yr+1day (§5.1 consistency, display).

**BACKLOG (real but needs design / decision / migration):**
- `brief/collectors/opportunity_cost.py:89` — `_MEANINGFUL_DELTA` compares absolute return, no current-holding baseline (needs baseline fetch).
- `portfolio/paper_trade.py:262` — `has_enough_paper_weeks` conflates run-count with weeks (gates M13.8 — re-verify intent).
- `position_monitor/service.py:197-206` — multi-lot LEFT JOIN picks an arbitrary lot (needs aggregation design).
- `regime/indicators.py:19` — `load_from_db` dead code (decide remove vs wire).
- `tax/cgt.py:118` — break-even formula cites a non-existent spec home → **spec amendment** (non-negotiable #8).
- `tax/lots.py:100` — ignores `cost_base_div296` (§6.4) → part of the already-tracked TC-20 unimplemented work.
- `ingestion/corporate_actions.py:171` — single-endpoint failure swallowed without logging.
- `ingestion/fundamentals.py:55` — `market_cap` NUMERIC(18,6) caps <10^12 (migration to widen, as 0028 did for research store).
- `cli/theme.py:78` — broad `except (ValueError, Exception)` (narrow it).

**→ BACKLOG (coverage):** the ~18 untested MED findings are folded into `docs/backlog-test-coverage.md`.

## MED (39)

- **[firewall] `asxos/cli/tax.py:12,140`** — tax CLI entry points untested
- **[firewall] `asxos/cli/thesis.py:121-532`** — Inconsistent personal-use gating: update-consensus/log-analyst/set-earnings call _require_personal_use() but the 9 core thesis commands (open/enter/revise/review/hold/exit/attach-underlying/show/list/history) do not.
- **[spec-deviation] `asxos/domain/tax/cgt.py:118-128`** — Break-even formula at lines 118-128 cites 'spec §2 + §5.1' but neither section defines a break-even formula; the derivation has no governing spec home (non-negotiable #8).
- **[spec-deviation] `asxos/domain/tax/lots.py:24,100-101`** — _selection/min-CGT compute gain only from cost_base_normal; cost_base_div296 (spec §6.4) is never read, so the module cannot source the Div 296 disposal branch (§6.4 'both calculations run independently').
- **[determinism] `asxos/brief/compose.py:184-188`** — Regime query `SELECT regime FROM signals WHERE as_of = $1 LIMIT 1` (lines 184-188) lacks model/model_version filter and ORDER BY; LIMIT 1 returns an arbitrary, non-reproducible row across the (model, model_version, symbol) rows sharing that as_of.
- **[determinism] `asxos/domain/position_monitor/service.py:202-206`** — Selection is non-deterministic: ORDER BY t.opened_at DESC LIMIT 1 disambiguates only the thesis, leaving lot choice undefined across runs.
- **[hard-fail-softened] `asxos/cli/theme.py:78-80`** — hard-fail-softened: `except (ValueError, Exception) as exc:` at line 78 catches every exception and reduces it to a one-line `console.print("[red]Error:[/red] ...")` + `raise typer.Exit(1)`, swallowing tracebacks for non-user failures, inconsistent with the narrow catches elsewhere in the file.
- **[hard-fail-softened] `asxos/ingestion/corporate_actions.py:171-181, 194-196`** — fetch() swallows per-endpoint exceptions to None with no logging; a symbol is only counted 'failed' when BOTH endpoints error. A single-endpoint failure (e.g. dividends() raises -> div=None while splits() returns []) silently drops that endpoint's data, is not counted failed, and emits no log/metric. The module has no logging at all, so partial loss is untraceable.
- **[bug] `asxos/cli/tax.py:185`** — Line 185 366-day eligible_at
- **[bug] `asxos/domain/brief/collectors/opportunity_cost.py:24,87-91`** — _MEANINGFUL_DELTA documented as a '>5% delta' (advantage over current holding) but line 89 compares the alternative's absolute net_expected_return against it; no current-holding baseline is fetched/subtracted, so the threshold is not a delta.
- **[bug] `asxos/domain/brief/collectors/tax_operational.py:57,69-73`** — regulatory_events.relevance_tags is JSONB DEFAULT '[]' (array) but collector treats it as a dict; default/array-shaped rows are silently dropped, emitting no regulatory item.
- **[bug] `asxos/domain/brief/collectors/wealth_state.py:98-110`** — Concentration total-MV denominator mismatch: per-holding numerator from current_holdings JOIN prices (line 46), denominator `mv` from snapshot holdings_mv_aud (line 66).
- **[bug] `asxos/domain/models/train.py:159-185`** — Empty walk_forward_split result leaves aucs/rmses empty; np.mean(aucs)/np.std(aucs) (lines 183-184) return nan silently instead of failing loudly, returning a degenerate TrainingResult.
- **[bug] `asxos/domain/portfolio/monitor_loader.py:105-106`** — prob_up and expected_return are mapped via _dec() with no None guard (lines 105-106), unlike every other numeric column in the loop; a NULL source value raises InvalidOperation and crashes the whole load.
- **[bug] `asxos/domain/portfolio/paper_trade.py:262-270`** — has_enough_paper_weeks conflates run-count with weeks-of-evidence: calls list_evaluable_runs(weeks=min_weeks) then returns len(runs) >= min_weeks, an accidental stricter composite gate.
- **[bug] `asxos/domain/position_monitor/service.py:197-206`** — load_position_context() LEFT JOINs holding_lots with no aggregation; cost_per_share/CGT date reflect one arbitrary lot when >1 open lot exists.
- **[bug] `asxos/domain/signals/feature_engine.py:122-198`** — No ordering guard: all lookback features rely on df being pre-sorted ascending by (symbol, dt). compute_all_features neither sorts nor asserts monotonic dt per group, so an unsorted input silently yields wrong-but-finite momentum/volatility/trend/ATR values.
- **[bug] `asxos/domain/tax/positions.py:64`** — CGT-branch Medicare at positions.py:64 uses hardcoded MEDICARE_LEVY_RATE (0.02) via medicare_levy_on() and ignores config.medicare_levy_rate, inconsistent with the dividend path which honors it.
- **[untested] `asxos/api/main.py:18-48`** — No test covers api.main lifespan, _check_migration_drift, or REQUIRED_MIGRATIONS — neither the success path nor the RuntimeError branches.
- **[untested] `asxos/cli/holdings.py:13-114`** — No test exercises import_holdings, _run_import_holdings, _ensure_in_universe, _resolve_fx_rate, or _infer_currency — leaving the USD->AUD FX path, missing-FX RuntimeError, div296 substitution, universe-insert idempotency, and currency suffix logic untested.
- **[untested] `asxos/cli/journal.py:16-191`** — No test file references journal_add/journal_list/journal_review or _run_journal_* helpers; all three CLI commands and their async DB paths, the symbol='-' sentinel-to-NULL conversion (line 27), and action.upper() validation (lines 24-26) are uncovered.
- **[untested] `asxos/cli/model.py:14-101`** — No tests reference model_activate, model_list, _run_model_activate, or _run_model_list; activate-not-found exit path, transaction flip, and empty-rows path are uncovered
- **[untested] `asxos/cli/news.py:17-90`** — Neither news_signoff nor _run_news_signoff has any covering test; --force bypass, the no-recent-ingest Exit(1) path, the decisions INSERT...RETURNING id, and the note-append logic are all unverified.
- **[untested] `asxos/cli/portfolio.py:23-427`** — No test module imports or exercises any CLI command in asxos/cli/portfolio.py; CLI-layer branches (date parsing 38-50, dry_run->no_persist 35-36, --side validation 141-142, CSV export 190-203, signoff 4-week gate 400-408) are untested at the CLI layer.
- **[untested] `asxos/cli/position.py:41-219`** — No test exercises this CLI module (lines 41-219); only the domain layer is covered.
- **[untested] `asxos/cli/profile.py:18-213`** — No test references this CLI module; all four commands and async runners (init/show/activate/list), the float->Decimal(str) path, comma-split exclusion parsing, do_activate branch, p-is-None exit-1 path, and empty-list early return are uncovered.
- **[untested] `asxos/cli/thesis.py:1-719`** — No test exercises the CLI module: tests cover asxos.domain.theses.service directly, with no CliRunner invocation of the typer commands, parsing helpers, alias registration, or redeploy rendering.
- **[untested] `asxos/domain/brief/collectors/tax_operational.py:23-95`** — collect_tax_operational has no covering test; test_brief_composer.py only uses the literal 'tax_operational' label.
- **[untested] `asxos/domain/brief/collectors/underlying_drivers.py:26-110`** — collect_underlying_drivers (lines 26-110) has no covering test; test_brief_v2_sections.py only exercises cross_layer_observations directly, never the collector. Untested: no-rows no_data branch (47-53), per-thesis scoring loop (66-91), empty-items no_data branch (100-105), driver-string formatting (84).
- **[untested] `asxos/domain/brief/collectors/wealth_state.py:23-134`** — collect_wealth_state has no covering test; test_brief_composer.py only uses the literal string 'wealth_state' and never invokes the collector. Branches (no_data, cash-ratio, drawdown, FX conversion, concentration loop, FX P&L) untested.
- **[untested] `asxos/domain/portfolio/build.py:272-355`** — persist() (lines 272-355) has no covering test: idempotent DELETE-then-reinsert, RETURNING run_id, both executemany batch inserts (target_allocations, proposed_trades), and the target_aud = target_weight * capital_aud arithmetic at line 318 are untested. tests/test_portfolio_build.py only covers build() hard-fail gates.
- **[untested] `asxos/domain/portfolio/monitor_loader.py:41-249`** — No test coverage for load_run_inputs/_load_benchmark/list_persisted_runs; the no-lookahead bound, entry-anchor gate, and equal-weight proxy growth index are untested.
- **[untested] `asxos/domain/regime/indicators.py:19-56`** — No test exercises load_from_db; the hard-fail branches (row is None, missing_critical) and the Decimal coercion at line 47 are uncovered.
- **[untested] `asxos/domain/tax/cgt.py:110-140`** — cgt_break_even_price (lines 110-140) has no covering test in tests/; it is live in production via position_monitor/display.py:223.
- **[untested] `asxos/domain/themes/service.py:304-314 (attach_thesis theses.themes sync)`** — The denormalised theses.themes array sync inside attach_thesis (UPDATE theses SET themes = array_append(...) WHERE status NOT IN ('exited','expired') AND NOT ($1 = ANY(themes))) has no covering test.
- **[untested] `asxos/domain/theses/service.py:568-697`** — update_analyst_consensus, log_analyst_action, and set_earnings have NO covering tests; their existence checks, diff serialisation, and transaction behaviour are unverified.
- **[dead-code] `asxos/domain/regime/indicators.py:19-56`** — load_from_db is defined but has zero callers anywhere in asxos/ or tests/; module docstring claims it is a read-path helper but nothing imports it.
- **[doc] `asxos/domain/tax/medicare.py:22-24`** — Docstring (lines 22-24) claims setting config medicare_levy_rate=0 handles the low-income threshold, but medicare_levy_on multiplies by the module constant MEDICARE_LEVY_RATE, not config.medicare_levy_rate, so the escape hatch is non-functional on the CGT path.
- **[smell] `asxos/ingestion/fundamentals.py:55,89-93`** — market_cap written to fundamentals.market_cap NUMERIC(18,6) caps representable value at <10^12; research-store equivalent was widened to NUMERIC(24,6) in migration 0028 but base fundamentals.market_cap was not, risking numeric overflow on trillion-dollar or cents-denominated values.

## LOW (40)

- **[spec-deviation] `asxos/domain/tax/lots.py:24,100-101`** — min-CGT search fixes the partial draw on the last lot of each combo (combinations preserve input order, _draw draws in fixed order), so it does not explore which lot bears the partial and post-discount gain is not truly minimised when per-unit gains differ across a partial combo.
- **[determinism] `asxos/cli/position.py:112-113, 121-130, 167-168`** — Sentiment inputs round-trip through float before Decimal (lines 112-113, 121-130, 167-168), unlike volume/short-interest which go str->Decimal directly (138-156).
- **[determinism] `asxos/domain/portfolio/allocator.py:60-61`** — position_count_for (lines 60-61) casts the interpolation fraction to float (`t = float((risk_scalar - x0) / (x1 - x0))`) and does float arithmetic in `round(y0 + t * (y1 - y0))`, contradicting the module docstring's 'All math is Decimal-only; no numpy' (line 4) and the portfolio-conventions 'Decimal-only arithmetic' rule that names allocator.py explicitly.
- **[hard-fail-softened] `asxos/cli/thesis.py:452-453`** — _show_redeploy_candidates wraps the whole DB fetch + render in 'except Exception' and downgrades any failure to a yellow warning, swallowing genuine infra errors.
- **[hard-fail-softened] `asxos/domain/brief/collectors/opportunity_cost.py:46-55`** — Broad except Exception string-matching 'does not exist'/'relation' to return no_data is fragile; an unrelated error mentioning 'relation' would be swallowed. A SQLSTATE 42P01 check would be precise.
- **[hard-fail-softened] `asxos/domain/brief/composer.py:142-143`** — _persist_brief_run() wraps its body in `try/except Exception: pass`, swallowing all exceptions with no logging — a persistent persistence failure is silently invisible.
- **[bug] `asxos/cli/position.py:79`** — `run_date = date.fromisoformat(as_of_str)` (line 79) is unwrapped; invalid --as-of raises uncaught ValueError producing a raw traceback instead of the clean red-message + Exit(1) used at 84-86, 91-93.
- **[bug] `asxos/clients/fred.py:97`** — Line 97: Decimal(raw) raises decimal.InvalidOperation on empty-string or non-'.' malformed FRED values, crashing get_series; only the exact '.' sentinel is handled.
- **[bug] `asxos/domain/brief/collectors/wealth_state.py:44-52`** — current_holdings JOIN prices p ON p.dt = as_of is an INNER JOIN; any held symbol without a price row for as_of is silently omitted from the concentration loop, undocumented in the docstring.
- **[bug] `asxos/domain/models/train.py:103`** — test_size = n // (n_splits+1) truncates to 0 when n <= n_splits with no guard; MIN_SAMPLES:1000 from ml-conventions is not enforced here, so an undersized panel yields a degenerate model rather than an explicit insufficient-data error.
- **[bug] `asxos/domain/portfolio/monitor_loader.py:199-211`** — The proxy benchmark is a changing-composition equal-weight average (filters only adj_close IS NOT NULL at 206, symbols drop out on dates with no row), so AVG re-bases to survivors; the note (218-220) overstates it as a fixed-basket TR index.
- **[bug] `asxos/domain/position_monitor/display.py:58-61`** — Header padding pad-13 can go negative, misaligning box right border; max(0,...) guards pad not pad-13.
- **[bug] `asxos/domain/research/alpha_eval.py:190-191`** — monotonic_frac drops NaN buckets before np.diff (line 190), so an interior NaN bucket makes two non-adjacent buckets appear adjacent, distorting the adjacent-decile step sequence.
- **[bug] `asxos/domain/underlyings/service.py:104,145`** — attach_underlying docstring claims ValueError for underlying_code not found, but code raises RuntimeError
- **[bug] `asxos/ingestion/financial_statements.py:229-234`** — fetch() bare except Exception swallows all provider/network/parse errors into a silent 'failed' count with no logging, making systematic outages (expired API key, schema drift) undetectable from job output.
- **[bug] `asxos/ingestion/news.py:215-226`** — ON CONFLICT (url) DO UPDATE does not refresh title, content_snippet, or published_at; re-ingested corrections are silently discarded (first-write-wins), and this is undocumented.
- **[bug] `asxos/ingestion/regulatory.py:189-201`** — upsert_events returns len(payload) (events sent), not rows actually affected, despite docstring 'Returns rows affected'. asyncpg executemany returns None and ON CONFLICT DO UPDATE counts can differ; job completion metrics overcount.
- **[bug] `asxos/ingestion/regulatory.py:81,89-91`** — Explicit symbol path in parse_json_announcements (line 81) is not validated against _SYMBOL_RE, so garbage 'symbol' values pass straight into relevance_tags, unlike the extract_symbols path.
- **[bug] `asxos/ingestion/regulatory.py:25,107`** — extract_symbols regex (3-5 uppercase letters) plus an 11-acronym denylist captures common CAPS English words (e.g. CEO, EPS) as false-positive tickers.
- **[bug] `asxos/jobs/utils/fallback_email.py:81-106`** — _record_fallback_failure calls init_pool() unconditionally (line 81), overwriting the module-global asxos.db._pool that compose_brief.main already created; init_pool() has no reuse guard (db.py:11-20), so the original pool is orphaned and the finally-block close_pool() (line 106) closes the NEW pool. Latent footgun for any future non-terminal caller; impact contained today because the path is terminal.
- **[bug] `asxos/jobs/utils/job_monitor.py:63-80`** — Stale-reset UPDATE only fails a prior 'running' row older than 2h; the subsequent INSERT...ON CONFLICT DO UPDATE unconditionally resets any existing (job_name, as_of) row to status='running' and nulls error/finished/rows_written, with no 'running'-guard or advisory lock, so a concurrent second instance <2h after the first clobbers its state.
- **[untested] `asxos/api/routes/health.py:9-16`** — Health route returns raw dicts via JSONResponse instead of a declared Pydantic response model, deviating from api-conventions.md §Routes ('Pydantic models for every request and response body. No raw dict/Any.')
- **[untested] `asxos/brief/email.py:33-50`** — send_brief() is never invoked directly by any test; the refusal guard (lines 38-41) and subject construction (line 35) are uncovered.
- **[untested] `asxos/cli/signal.py:11-61`** — Neither signal() nor _run_signal() has any covering test; row-None Exit(1), str/dict shap handling, abs-sorted SHAP ordering/truncation, and bias/None filtering are all untested.
- **[untested] `asxos/clients/fred.py:46-110`** — No test exercises FREDClient, get_series, latest_value, _get, _is_retryable, or get_client (lines 46-110, 113-121).
- **[untested] `asxos/domain/portfolio/volatility.py:63-108`** — load_vols_for_symbols (async DB entry point: empty-symbols early return, ROW_NUMBER/ANY($1::text[]) query, per-symbol grouping, ValueError silent-omit loop at 104-107) has no covering test.
- **[untested] `asxos/domain/position_monitor/fetcher.py:131-180`** — The two public async entry points fetch_price_data() (lines 131-161) and fetch_macro_data() (lines 164-180) have no covering tests; tests/test_position_monitor.py covers only the pure helpers.
- **[untested] `asxos/domain/research/alpha_eval.py:259-321`** — evaluate() only computes deciles for prob_col, never iterating score_cols, despite the module/evaluate docstring promising score-variant comparison across prob_up/composite/expected_return.
- **[untested] `asxos/domain/research/alpha_loader.py:69-84,142-159`** — load_panel and load_factor_panel (SQL panel construction, NUMERIC->float coercion, empty-DataFrame return, rn0+h join correctness) are entirely untested
- **[untested] `asxos/domain/signals/loader.py:42-64, 122-251`** — The include_adj_close=True / price_basis="adj_close" v1_6 shadow path (loader.py lines 56-57, 68-76, 85, 134-135, 170-171) has no test that executes it at runtime; only the default close-basis panel is exercised.
- **[untested] `asxos/domain/signals/loader.py:128, 136-150, 177-188, 230-234`** — The `symbols is not None` batched-load path (the three `= ANY($n)` SQL branches for prices line 144, fundamentals line 183, caps line 232) has no test executing it; all loader tests use the symbols=None branch.
- **[untested] `asxos/domain/tax/lots.py:105-106`** — Line 24 divides cost_base_normal by lot.quantity with no zero guard; a zero-quantity lot raises ZeroDivisionError instead of a domain error, and partial-draw brokerage proration is implicit/undocumented.
- **[untested] `asxos/domain/underlyings/service.py:241-280`** — get_5d_moves (lines 241-280) has no direct unit/integration test
- **[untested] `asxos/ingestion/fundamentals.py:65-93`** — fetch_and_upsert_fundamentals has no direct covering test: test_symbol_failure_does_not_raise patches it with a side_effect, so the actual INSERT...ON CONFLICT SQL, $1..$9 parameter ordering, and EXCLUDED upsert branch are never executed.
- **[untested] `asxos/ingestion/news.py:185-230`** — upsert_news() has no test exercising real behaviour; the only test reference (tests/test_ingest_news_job.py:60) patches it out with AsyncMock, leaving the SQL string, jsonb/numeric casts, payload tuple construction, and the symbols-merge ON CONFLICT logic unverified.
- **[doc] `asxos/domain/brief/collectors/watchlist.py:7-9,65`** — Module docstring documents a 'yellow' SeverityLevel (stop breached by current price) that is never emitted; line 65 hardcodes green and the SQL never fetches a current price to compute the breach.
- **[smell] `asxos/cli/model.py:27-52`** — Line 97 falsy-check on roc_auc NUMERIC renders Decimal('0') as empty string instead of '0.0000'; should be 'is not None'
- **[?] `asxos/cli/signal.py:`** — Line 50/53 float() on raw JSONB shap value can raise ValueError on non-numeric string after the DB pool is already closed; only v is not None is filtered.
- **[?] `asxos/domain/signals/regime.py:`** — Lines 72-73: recent_sma slice can contain NaN for panels of length 200..218 because sma uses min_periods=200; _slope returns NaN; function silently falls back to 'neutral' with no signal to caller.
- **[?] `asxos/ingestion/eodhd.py:`** — exchange_symbols/daily_prices/daily_prices_bulk/fundamentals/dividends/splits return _get() directly with `# type: ignore[no-any-return]` and no isinstance guard, while only news_for_symbol (107) and sentiments_for_symbol (127) defend with `result if isinstance(result, list) else []`. A dict error/no-data envelope from EODHD would propagate to list-expecting callers and fail later with an opaque error — inconsistent defensive handling across sibling methods.

## INFO (23)

- **[security] `asxos/clients/fred.py:67`** — Line 67: api_key passed as URL query param; on HTTPStatusError the URL (incl. api_key) can appear in httpx repr/str and be reraised (reraise=True) and logged at call sites.
- **[firewall] `asxos/cli/position.py:68, 207`** — Both CLI entry points (monitor line 68, history line 207) call `_require_personal_use()` before doing work — firewall gate present per portfolio-conventions Part 0 Q1. Positive observation.
- **[firewall] `asxos/cli/tax.py:12,140`** — Missing _require_personal_use() firewall gate on tax CLI
- **[firewall] `asxos/domain/portfolio/paper_trade.py:170-312`** — No _require_personal_use()/ASXOS_PERSONAL_USE gate in this module; flagged as info only to confirm the firewall belongs on the CLI command, not this helper layer.
- **[firewall] `asxos/domain/portfolio/profile.py:1-231`** — No _require_personal_use / ASXOS_PERSONAL_USE gate in profile.py. This is the service/persistence layer; the docstring states CLI callers gate before invoking. Per portfolio-conventions.md the firewall belongs on CLI entry points and jobs, NOT the service layer, so absence here is intended, not a defect.
- **[firewall] `asxos/domain/research/factor_scores.py:318`** — refresh_factor_scores is a service-layer coroutine, not a CLI/job entry point, so the personal-use firewall gate's absence here is intended; caller supplies the gate.
- **[spec-deviation] `asxos/domain/tax/medicare.py:22-24`** — Spec §7.1 low-income shading not implemented in v1; base*0.02 applied unconditionally for individuals. Documented v1 simplification, accepted scope limitation, conformance gap noted.
- **[determinism] `asxos/brief/compose.py:269-273`** — Top-factor selection in `_signal_changes` (lines 269-273) casts shap JSONB values to float for sorting/formatting; equal-abs-magnitude factors have a tiebreak that depends on sort stability / dict iteration order — a minor display nondeterminism.
- **[determinism] `asxos/domain/brief/collectors/tax_operational.py:23-95`** — No Decimal/numpy arithmetic in module; pure I/O + delegation to days_to_eligibility (calendar-based per spec §5.1). No determinism concern.
- **[determinism] `asxos/domain/brief/collectors/underlying_drivers.py:84,88`** — Float format specs :+.1f (line 84) and :+.2f (line 88) applied to underlying movement values; worth confirming upstream values are Decimal not float to keep the Decimal-only domain float-free.
- **[determinism] `asxos/domain/position_monitor/display.py:170`** — float(contrib) used at line 172 only to choose the icon via contrib_f > 0; float creep in a Decimal-domain area.
- **[determinism] `asxos/domain/position_monitor/display.py:278, 281, 285-286`** — format_history uses float() on weighted_movement, current_price, retail_ratio, news_sentiment purely for f-string number formatting.
- **[determinism] `asxos/domain/research/alpha_eval.py:30-31, all`** — numpy/pandas float math is used throughout, but this is an explicitly-sanctioned research module, not a Decimal-only-rule violation.
- **[determinism] `asxos/domain/research/factor_scores.py:30-31, 224-228, 254, 264`** — Module uses plain float/statistics rather than Decimal; explicitly sanctioned by docstring (dimensionless ranking scores, not money), quantised to NUMERIC(18,6) via _q6 before write. Not a violation.
- **[determinism] `asxos/domain/signals/regime.py:35-45`** — Lines 35-45: module uses float/numpy (astype(float), np.polyfit, proxy.mean()). Self-described NON-violation recording that numpy is permitted in signals domain; Decimal-only applies to M13 portfolio modules only. np.polyfit deterministic.
- **[determinism] `asxos/domain/signals/writer.py:43-44,51,60-61`** — Module uses numpy/pandas float coercion (to_numpy(dtype=float), float(prob_up[i]), float(v)). Recorded as info-only; ml-conventions.md sanctions numpy in the signals layer and asyncpg handles numpy natively, so no psycopg2 adapter is needed.
- **[bug] `asxos/api/main.py:25`** — Migration drift guard uses `count < REQUIRED_MIGRATIONS`, catching only 'behind' drift, not 'ahead' drift.
- **[bug] `asxos/brief/email.py:62-71`** — _send_via_resend only handles dict responses; an object-typed SDK response yields message_id=None, masking a successful send's id (lines 62-71).
- **[bug] `asxos/cli/profile.py:25`** — Monetary capital/cash_floor/leverage/caps/min_position taken as CLI float then converted via Decimal(str(x)), inconsistent with the Decimal-only invariant; precision smell.
- **[bug] `asxos/cli/thesis.py:86-90`** — Entry-band falsy check 'if t.entry_band_lower and t.entry_band_upper' (line 88) and line 692 treat a legitimate Decimal('0') edge as absent and render the em-dash; should be 'is not None'.
- **[bug] `asxos/domain/portfolio/constraints.py:132-133`** — Line 132-133: builtin sum() starts at int 0, so an empty targets list yields int current_sum and a Decimal-minus-int subtraction; latent only because allocator hard-fails on empty universe upstream and Decimal-int is valid Python.
- **[bug] `asxos/ingestion/regulatory.py:176-188`** — payload built as 6-tuples then re-tupled at line 199 applying _json to p[5]; redundant parallel structures, a duplication smell, no correctness break.
- **[?] `asxos/cli/signal.py:`** — NUMERIC(18,6)/Decimal columns coerced to float for display (abs(float(kv[1])), float(v):+.3f, prob_up:.3f, expected_return:+.4f); display-only but float creep and +.4f truncates NUMERIC(18,6) precision.
