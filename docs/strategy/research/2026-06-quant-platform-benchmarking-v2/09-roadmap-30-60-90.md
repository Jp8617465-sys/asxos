# 09 — 30 / 60 / 90 Day Roadmap (modern methods)

Correctness and evaluation precede modelling. Each item: priority · why · dependency · expected output · success benchmark.

## Days 0–30 — Correctness & data trust (no new models)
1. **[P0] Verify `expected_return` units.** *Why:* labels may be degenerate (`06` R1). *Dep:* none. *Output:* verdict memo + distribution. *Success:* CONFIRMED/REFUTED.
2. **[P0] Verify + adopt `adj_close`.** *Why:* #1 ASX fix — franked ex-dates/rights/splits manufacture spurious returns (`06` R2). *Dep:* none. *Output:* ex-date contamination quantified. *Success:* adopt-`adj_close` decision.
3. **[P1] Universe survivorship plan.** *Why:* `is_active`-on-history + no AU size premium doubly inflates backtests (`06` R4). *Output:* `universe_history` design. *Success:* as-of membership plan agreed.
4. **[P1] Baseline definitions** (equal-weight, 12-1 momentum, trend, value/quality, persistence, random) + adopt **Open Source Asset Pricing** as the benchmark library. *Output:* design doc.
5. **[P1] Brief caveat (spec).** *Output:* `prob_up`/`confidence`/caveat display copy.

## Days 31–60 — Evaluation harness (the missing core)
6. **[P1] Baseline suite** (`research/baselines.py`). *Dep:* #2,#4. *Success:* reproducible + tested.
7. **[P1] Rank IC + net-of-cost decile spreads** for signal vs baselines (`research/metrics.py`). *Dep:* #6. *Success:* **first real "useful or coin flip" answer.**
8. **[P1] Calibration check** (reliability + Brier; isotonic/Platt option). *Dep:* #1. *Success:* calibration measured; threshold-review *proposal* (no change).
9. **[P1] `signal_outcomes` provenance + read-back** into a rolling rank-IC brief footnote. *Dep:* #1,#2. *Success:* outcomes trusted + surfaced.
10. **[P1] Simple net-of-cost economic backtest** (top-decile vs AXJO). *Dep:* #6,#7. *Success:* net-of-cost IR computed.

## Days 61–90 — Robust backtest, paper book, governance
11. **[P2] Purged/embargoed walk-forward → CPCV; Deflated Sharpe + PBO; FDR trial counting.** *Dep:* #6–10. *Success:* leakage closed; deflated-significant edge or honest null.
12. **[P2] Denoised-covariance HRP/Schur sizing (`skfolio`) + L1 turnover penalty + ADV cap + CGT/franking overlay.** *Dep:* #11. *Success:* tradeable, after-tax sizing.
13. **[P2] Paper portfolio / decision ledger + attribution.** *Dep:* #11,#12. *Success:* beats equal-weight/momentum paper books after cost/tax.
14. **[P2] Promotion/kill gates + Evidently drift + `review_queue`.** *Dep:* #7–13. *Success:* economic promotion gate + kill criteria + re-validated activation + drift alarms.

**Quarter success benchmark:** *does the signal beat 12-1 momentum and equal-weight on net-of-cost rank IC / decile spread, OOS (purged), survivorship-controlled, Deflated-Sharpe-significant?* Yes → upgrade to **validated research signal** → paper book. No → **quarantine as research-only**; keep the brief honest.

**Adjustment from findings:** the data-trust gates are already merged (`dcb0461`, `a719b3c`), so Days 0–30 lead with the **units + `adj_close`** correctness fixes (modern ASX evidence elevates `adj_close` to a co-#1 priority alongside units).
