# 09 — 30 / 60 / 90 Day Roadmap

Ordered so **correctness and evaluation precede any modelling**. Each item: priority · why · dependency · risk · expected output · success benchmark.

## Days 0–30 — Correctness & data trust (no new models)

1. **[P0] Verify `expected_return` units.** *Why:* labels may be degenerate (`06` Risk 1). *Dep:* none. *Risk:* low (read-only). *Output:* verdict memo + `signals.expected_return` distribution. *Success:* units conclusively CONFIRMED/REFUTED.
2. **[P0] Verify `adj_close` vs `close`.** *Why:* corporate-action noise (`06` Risk 2). *Dep:* none. *Output:* ex-date contamination quantified. *Success:* documented decision to adopt `adj_close`.
3. **[P1] Confirm completeness/recency gates in production** (read-only `job_runs`/log review). *Output:* gate behaviour confirmed. *Success:* gate fires correctly on residue/stale days.
4. **[P1] Define baselines (spec only)** — equal-weight, 12-1 momentum, trend, value/quality, persistence, random. *Output:* baseline design doc. *Success:* agreed definitions + forward-return convention.
5. **[P1] Brief caveat (spec).** *Why:* over-confident UI (`01` §5). *Output:* proposed caveat + `prob_up`/`confidence` display copy (no code change yet). *Success:* truthful display drafted.

## Days 31–60 — Evaluation harness (the missing core)

6. **[P1] Implement baseline suite** (`research/baselines.py`). *Dep:* #2, #4. *Output:* baseline forward returns. *Success:* reproducible + unit-tested.
7. **[P1] Rank IC + quintile spreads** for signal and baselines (`research/metrics.py`). *Dep:* #6. *Output:* IC time series + decile spreads. *Success:* **first real answer to "useful or coin flip"** — signal IC vs baselines.
8. **[P1] `signal_outcomes` provenance review + read-back.** *Dep:* #1-2 (clean provenance). *Output:* provenance memo + rolling hit-rate/IC for the brief footnote. *Success:* outcomes trusted and surfaced.
9. **[P1] Simple economic backtest** (long top-decile vs AXJO, gross then net). *Dep:* #6-7. *Output:* IR/turnover. *Success:* net-of-cost IR computed.
10. **[P1] Calibration check** (reliability + Brier on `prob_up`). *Dep:* #1. *Output:* calibration verdict + threshold-review *proposal* (no change). *Success:* calibration measured.

## Days 61–90 — Backtest, paper book, governance

11. **[P2] Walk-forward harness with purge/embargo, costs, turnover.** *Dep:* #6-9. *Output:* OOS net-of-cost IC/spread, deflated significance. *Success:* leakage closed; deflated-significant edge or honest null.
12. **[P2] Liquidity/capacity + tax/risk overlay** (ADV floor, shrinkage covariance, CGT-aware sizing). *Dep:* #11. *Output:* tradeable, after-tax sizing. *Success:* spreads within ADV caps; after-tax IR measured.
13. **[P2] Paper portfolio / decision ledger + attribution.** *Dep:* #11-12. *Output:* shadow book vs AXJO. *Success:* beats equal-weight/momentum paper books after cost/tax.
14. **[P2] Promotion/kill gates + `review_queue` / agentic review.** *Dep:* #7-13. *Output:* governance doc + monitors. *Success:* economic promotion gate + documented kill criteria + re-validated activation.

**Overarching success benchmark for the quarter:** *does the signal beat 12-1 momentum and equal-weight on net-of-cost rank IC / decile spread, out-of-sample, survivorship-controlled?* If yes → upgrade to **validated research signal** and proceed to paper book. If no → quarantine as research-only; keep the brief honest.

**Adjustment from findings:** the prior audit already merged the data-trust gates (`dcb0461`, `a719b3c`), so Days 0–30 lead with the **two correctness verifications** rather than re-doing completeness work.
