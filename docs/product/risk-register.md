# arbi risk register — known risks arbi tracks and surfaces

**Status:** current
**Scope:** the standing risks arbi carries into every brief until they're closed
**Last verified:** 2026-07-14 (post-merge reconciliation — #32/#33/#35/#31/#34/#36 merged)
**Docs-truth correction:** 2026-08-13 (`SB0-01` sweep — R5's branch-protection final state
corrected from "1 required approving review" to the settled `required_approving_review_count: 0`;
no risk opened, closed, or re-severitied. Rows R1–R16 otherwise unchanged and their own dates stand.)
· 2026-08-14 (`SB0-02` memory/eval/permission wording reconciliation — R16's present-tense
`".*"`-matcher claim corrected against `.claude/settings.json`, which now carries exact per-tool
matcher entries. **No risk opened, closed, re-severitied, or re-scoped; no control claimed as
mechanical that is not; no permission tier touched.** R16 stays **open at P1**.)
**Owner:** arbi maintains (I2, command-invoked); James owns the risk appetite
**Superseded by:** N/A

The durable risk list arbi surfaces (BLOCKERS / NEW BUGS-RISKS) and updates as risks open or
close. James owns the risk *appetite*; arbi owns *keeping the list honest and visible*.
Severity: **P0** (blocks real capital / foundation) · **P1** (blocks a phase) · **P2**
(quality/latent). Append/adjust rows; never silently drop a risk — close it with a dated note.

---

## Register

| ID | Risk | Sev | Status | Mitigation / next step | Source |
|---|---|---|---|---|---|
| R1 | **Model A horizon mismatch** — signal edge may collapse in <5d and reverse by 21d, vs multi-month holding | **P0** | **resolved 2026-07-11 (confirmed, against Model A)** | Decay check RAN directly on 19,032 matured `signal_outcomes`: `corr(ml_prob,21d)=−0.03`, STRONG_BUY 21d −0.09% vs HOLD +5.07% — no usable edge, conviction inverted. **Quarantine STANDS** (rule #11 → standing). Residual: `track_signal_outcomes` staleness — **CLOSED**: init-pool fix merged (PR #26, 2026-07-11) and the follow-on `AmbiguousParameterError` `::varchar` cast fix merged (PR #30, 2026-07-13); resumes at the next weekly Sun 03:00 UTC cron (the ~2026-03-26..04-11 gap is past the 90-day lookback and won't backfill). Strategic call MADE 2026-07-11: **SHELVE** (`ml-engine-shelf-2026-07-11.md`) | `docs/model-a-decay-analysis-2026-07-11.md` |
| R2 | **Agent DB role scoping** — agents' SELECT-only is prompt-enforced only; an injected agent could emit writes | P1 | open | Read-only Postgres role for agent MCP sessions (`m14_candidate_agent_db_role_scoping`) before Phase 2c | `portfolio-conventions.md`, `next-session-backlog.md:256` |
| R3 | **Allocator risk-blindness** — v1 allocator ignores ASX beta clustering; 30% sector cap ≠ diversification | P2 | open (accepted v1) | Market-beta cap `m14_candidate_beta_cap` (v2); cash floor + leverage cap are the only v1 protections | `portfolio-conventions.md:200-206` |
| R4 | **Dark-launched-≠-released** — M13/M14a are built but gated off; "built" can read as "live" | P2 | open | Track gate status in `roadmap-state.md`; don't count dark-launched work as delivered | V2 arch audit Part A |
| R5 | **Prompt-only enforcement** — every arbi boundary/tier is prompt+doc enforced today, not mechanical | P1 | partial (`unattended-guard.sh` pre-filters I5–I6 unattended; **PR-2 Permission Friction Pack, 2026-07-14** adds an always-on layer: settings `deny` array on authority paths + `authority-guard.sh`/`push-guard.sh`/`pr-draft-guard.sh` — mechanically blocks dangerous push/gh/PR-lifecycle shapes and authority-file writes attended too, not just unattended) | Managed Agents permission policies + read-only DB role; treat I5–I6 / P5–P6 as disabled until then; P6 (execution) is already enforced by no execution tool ever being mounted. **Not closed by PR-2:** push/PR-creation friction itself (still prompts — deny-only is a deliberate choice for asymmetric-risk reasons, not because hook-`allow` is unsafe/unconfirmed — it IS confirmed to work, per a 2026-07-14 correction below); the executor-arbitrary-code path (a pre-allowed test runner calling the GitHub/git API directly bypasses every Bash-string-matching hook) — real backstop is GitHub branch protection on `main`, still not configured. **Security review (2026-07-14) found and closed 3 real gaps in the first draft:** a bare shell redirect (`echo x > CLAUDE.md`) bypassing `authority-guard.sh` (no "recognized file command" involved); a wildcard-refspec/`remote.*.push`-config push bypassing `push-guard.sh`'s repo-wide-push check; `git -C`/`--git-dir` redirecting the same hook's branch check at an uninspected repo — all three fixed same-round, 107 manually-verified + formal pytest cases. **Update 2026-07-15: branch protection on `main` is PLAN-GATED, not just unconfigured** — GitHub reserves branch protection + rulesets for paid plans on private repos (API 403s identically to the UI; no workaround), and James declined the upgrade. Consequence: CODEOWNERS is **inert** on this plan (mechanical grader≠producer via required CODEOWNER review is unavailable); promotion stays a disciplined-process gate. Substitute controls: a detective `main-push-guard` Action (converts any direct push to `main` into a failed run + alert issue — drafted, **James to apply**: `.github/workflows/` is session-permission-denied, YAML embedded in `arbi-dream-automation-2026-07-15.md` §Amendment) now, and the **fork/machine-identity model** (automation identity has zero write on the canonical repo; cross-fork PRs only) as the server-side backstop precondition for any Phase-3 unattended write. **Update 2026-08-12 — the "PLAN-GATED / not configured" clauses above are SUPERSEDED and must not be cited again.** James activated GitHub Pro and branch protection went live on `main` 2026-07-17 (ruleset: require PR, require `full-check`, **0 required approvals**, block force-push, restrict deletions — confirmed enforcing, PR #53 sat `mergeable_state=blocked` until green; `session-handoff-2026-07-17.md:16`), and it was re-applied 2026-08-12 via the branch-protection API with ~~**1 required approving review + Code Owner review**~~, `enforce_admins: false` (`docs/proposals/arbi-guard-carveouts-2026-08-12.md` Artifact 1). **⚠️ CORRECTION 2026-08-13 (SB0-01): the "1 required approving review + Code Owner review" clause records an intermediate state that was REVERTED the same day. The settled 2026-08-12 configuration is `required_approving_review_count: 0`.** The 1-approval setting was tried and reverted because on a solo repo GitHub forbids self-approval, so requiring one turned every merge into an `enforce_admins:false` admin bypass — weaker audit evidence for zero added enforcement. Two independent records agree on `0`: `arbi-autonomy-loop.md:52-58` and `roadmap-state.md:694-696` (Last wake snapshot, "approvals=0 → CODEOWNERS advisory not mechanical"). ~~Consequences: CODEOWNERS is **no longer inert**~~ — **at 0 required approvals CODEOWNERS is ADVISORY, not mechanical**; the "no longer inert" conclusion followed from the reverted 1-approval premise and does not hold. What *does* hold from the 2026-08-12 update: branch protection is genuinely configured and enforcing (PR required, `full-check` required, deletion and non-fast-forward blocked; rulesets `asxos-main` 19077432 + `main` 18221894 live since 2026-07-17), so the detective `main-push-guard` Action substitute is no longer the only control and the executor-arbitrary-code residual has its named server-side backstop. Verify before relying on it: `gh api repos/Jp8617465-sys/asxos/branches/main/protection`. Three things this does NOT close: the residual itself (a pre-allowed runner calling the git/GitHub API still evades every Bash-string hook — protection binds the *ref*, not the session), and grader≠producer (a solo repo cannot satisfy a 1-approval requirement without an admin bypass, since GitHub forbids self-approval — see the carve-outs doc for the `0`-vs-`1` trade-off James still owns) **Update 2026-08-21 — this row's `partial` status was, for six weeks, weaker than it read.** Three of the five hooks this cell credits (`review-gate.sh`, `push-guard.sh`, `unattended-guard.sh`) anchored every path and branch lookup to `$CLAUDE_PROJECT_DIR`, so every guarantee named here was void for any tool call made inside a **git worktree** — the technique `arbi-run-ledger.md` itself recommends for parallel missions, so a live gap rather than a theoretical one. Concretely: staged Python committed ungated, a push from a worktree sitting on `main` unblocked, and authority- and capital-path edits ungated under `ARBI_UNATTENDED=1` — the inverse of that hook's fail-closed contract. Closed 2026-08-20 (PR #145): all three now resolve the payload's own `.cwd` to its git toplevel and test both that checkout and the control checkout, mirroring `authority-guard.sh` — the only one that was already correct. Verified live against a real git worktree (ALLOW before, DENY after) with eight primary-root regression cases unchanged; pinned by `tests/test_hook_worktree_scope.py`. **This RESTORES the coverage R5 already claimed — it does not close R5 and does not reduce its severity**, because it added no new enforcement surface; it made an already-credited one actually apply. Read alongside **R16** (`.*`-matcher hooks that never fire in a given harness) and **R17** (controls inert in the Cursor lane): all three are one family — *the control exists and is not applying in this execution context* — and that family, not prompt-vs-mechanical, is now R5's dominant residual. A fourth instance of it surfaced the same day: MCP tool calls sit outside PreToolUse's supported-tool list, so `pr-draft-guard.sh` did not fire when a PR was un-drafted via the GitHub MCP server. | `arbi-permission-model.md` §Runtime enforcement honesty; `docs/proposals/arbi-dream-automation-2026-07-15.md` §Amendment |
| R6 | **Reward hacking** — a naive self-improvement metric degrades arbi silently | P1 | mitigated-by-design | Multi-objective hard-gated scorecard + external grader + promotion gate | `arbi-scorecard.md`, `arbi-promotion-gate.md` |
| R7 | **Memory poisoning** — untrusted input written to a read-write store, later read as trusted | P1 | mitigated-by-design | Read-only authority stores; arbi never writes what it reads as authority | `arbi-memory-policy.md` |
| R8 | **Behavioral-only Model A quarantine** — nothing in code/DB enforced rule #11; migration 0032 grandfathered `model_a` to `approved_for_allocation=TRUE`, so `build()` would pass Model A to the allocator if invoked | P1 | **resolved 2026-07-11 (revoked, mechanically enforced)** | `UPDATE model_versions SET approved_for_allocation=FALSE WHERE model='model_a' AND version='v1_5'` run 2026-07-11 with James's explicit I5 approval (RETURNING confirmed 1 row; it is the only `model_versions` row → **0 approved-and-active**). Rule #11 is now MECHANICAL: `resolve_production_model()` resolves 0 approved → the allocator `build()` hard-fails (intended dormant state) instead of using v1_5. Safe to revoke because R9 (brief best-effort on 0-approved) landed first. **Follow-up (drafted, not yet built):** make the weekly `build_portfolio` cron classify the expected 0-approved RuntimeError as a distinct `dormant`/`blocked` job_runs status vs a generic `failure` — at the JOB layer, WITHOUT softening `build.py`'s hard-fail (which IS the enforcement). Reversible via re-approve if a future model earns it | scan `wf_f54323f5-d7d`; `build.py:205-219`, migration 0032; revoke 2026-07-11 |
| R9 | **Brief hard-couples to model approval** — V1 `collect()` (`compose.py:190`) + V2 `active_theses` collector call `resolve_production_model()` unconditionally (RuntimeError on 0 approved); revoking approval to harden the quarantine would hard-fail the whole brief / hide every thesis card — the Model-A-*independent* discipline layer | P1 | **fixed 2026-07-10** | DONE: `resolve_production_model()` gained `required=False`; both brief sites best-effort (skip Model A reads on 0/>1 approved), allocator keeps `required=True` hard-fail. Revoking approval now hardens the quarantine without hiding thesis cards. Tests: `test_production_gate.py` + flipped brief tests | scan `wf_f54323f5-d7d`; `production_gate.py`, `compose.py:190`, `active_theses.py` |
| R10 | **`cost_base_normal` currency is ambiguous** — it stores the **AUD tax base** for foreign holdings (HUBS.NYSE: 6978.23 AUD = 24 × US$187.54 ÷ 0.6450 acq-FX), but nothing in the column signals that. In the 2026-07-11 `/pm-review`, **2 of 5 analysis agents** (milestone, benchmark) read it as a USD cost total (÷24 = US$290.76) and reported HUBS **−29% / stop-violated / −28pp vs XJO** — all false; the position is ≈ flat (+9.8% USD). A single-agent review would have shipped that error into a capital-relevant memo. The acquisition FX (0.6450) is **confirmed** against the brokerage statement (James, 2026-07-11) — an ESPP fill FX differing from spot is expected, not an error — so the remaining issue is purely the column's currency **labeling**, not the number | **P1** | open | Make the currency explicit: a `cost_base_ccy` marker or split `cost_base_aud`/`_usd`; until then any consumer mixing `holding_lots` (native) with `portfolio_daily_snapshots` (AUD) must FX-convert. See cleanup-backlog RC1 | `/pm-review` 2026-07-11; agents milestone/benchmark/coherence; `holding_lots` id=1, thesis_revision #13 |
| R11 | **`conviction_level` is NULL on all 13 theses** — the size-vs-conviction discipline check (`portfolio-coherence-reviewer`) cannot run for *any* position, including the one name that is 100% of capital. A whole discipline dimension is unpopulated, so conviction→exposure coherence is uncheckable portfolio-wide | P2 | open | Backfill conviction on live theses; consider requiring it on `enter_thesis()`. See cleanup-backlog RC4 | `/pm-review` 2026-07-11; `portfolio-coherence-reviewer`; `theses.conviction_level` |
| R12 | **`asx theme` CLI has no s766B firewall gate** — `asxos/cli/theme.py` is the only file in `asxos/cli/` with zero `_require_personal_use()` calls; every sibling governance/portfolio CLI (`macro_thesis.py`, `thesis.py`, `position.py`, `holdings.py`, `portfolio.py`, `profile.py`, `agent_run.py`, `brief.py`, `news.py`) has it, and CLAUDE.md states the gate as a hard MUST for every CLI entry point touching portfolio/investment data. Found while adding the 4 new theme-governance CLI verbs (`approve`/`reject`/`holding approve`/`holding reject`) — those 4 are gated (matching `macro_thesis.py` precedent); the pre-existing 7 commands (`create`, `list`, `review`, `adjacency add`, `stage`, `attach`, `coverage`) are not, and fixing them is a separate, deliberate change (small but touches 7 sites — not bundled into the governance-verb PR to keep it scoped) | P2 | **resolved 2026-07-13 (PR #33, draft)** | DONE (PR #33): `_require_personal_use()` added to all 7 pre-existing theme commands — all 11 theme entry points now gated. Guarded by `tests/test_cli_theme.py` (parametrised over the 7; unset → non-zero exit before `init_pool`). Review loop clean (security/refactor/writer) | discovered 2026-07-11 landing `asx theme approve/reject`; fixed 2026-07-13 overnight |
| R13 | **review-gate hook bypassable via a compound `git add && … && git commit`** — `.claude/hooks/review-gate.sh` checks *staged* `.py` at PreToolUse time; if the `git add` is in the SAME Bash command as `git commit`, nothing is staged when the hook evaluates → the commit proceeds ungated. Confirmed inadvertently 2026-07-13 (M1 commit `1cb016d`; the review loop DID run, so no harm this time, but the mechanical gate was not enforced) | P2 | **resolved 2026-07-14 (PR #35 merged)** | DONE: hook denies up front when a command stages Python in the same step as the commit — compound `git add`/`git stage` + commit, and auto-stage `-a`/`-am`/`-am"msg"`/`--all` (bare `--amend` exempt) — whenever pending `.py` exists. 15 subprocess tests drive the real hook (`tests/test_review_gate_hook.py`); security-engineer confirmed injection-safe. Residual bypasses (`git -c … commit`, multi-space `git  commit`, pathspec `git commit x.py`, file created in-compound) are pre-existing, need real command parsing, and are documented in the hook header — acceptable for an advisory speed-bump with a forgeable marker | discovered 2026-07-13 overnight run; fixed PR #35, merged 2026-07-14 |
| R14 | **s766B firewall convention not path-attached to `asxos/cli/`** — `portfolio-conventions.md` states "every CLI entry point MUST call `_require_personal_use()`" but is scoped "Read before touching `asxos/domain/portfolio/`", so it doesn't auto-surface when editing the CLI files where the entry points live. Arguably the root cause of R12; could recur for future CLI commands | P2 | open | Attach a firewall rule to `asxos/cli/**`, or a lint/test asserting every `@*_app.command` carries the gate. Owner: system-architect | discovered 2026-07-13 (R12 review) |
| R15 | **PR #39 overcorrected merge safety — attended merge execution removed** — the bare-tool-name deny of `mcp__github__merge_pull_request` (plus `pr-draft-guard.sh`'s unconditional merge deny) blocked the established James-instructed attended merge pattern (James names the PR, agent re-checks green/clean/not-draft, merges — the PR #26 and six-PR-train precedent), not just agent-initiated merges. Over-blocking risk (workflow lockout), not under-protection; same class as the `Edit(/.claude/**)` self-lockout the #39 session already hit once | P2 | **fix drafted 2026-07-14 (restore-attended-merge PR)** | Split the policy: agent-initiated merge forbidden (instruction-level); James-instructed attended execution allowed via normal permission flow (no standing/hook allow); unattended merge hook-denied (`ARBI_UNATTENDED=1`); auto-merge denied every mode (settings + hook). settings.json drops only the `merge_pull_request` bare deny; `enable_pr_auto_merge` deny stays. I6 unchanged (`always_ask`, never standing) | James correction 2026-07-14; decision-log `restore-attended-merge-2026-07-14` |
| R16 | **`.*`-matcher PreToolUse hooks are INERT in the web/remote environment** — sharpens R5. **⚠️ READ FIRST — CORRECTION 2026-08-14 (`SB0-02`): every present-tense matcher claim in this cell describes the 2026-07-16 configuration and is SUPERSEDED. Fix (1) HAS LANDED. See the dated correction at the end of the mitigation cell before citing any part of this row. The row remains OPEN at P1 — the correction narrows what is left, it does not close it.** Three of the five PreToolUse guards use `"matcher": ".*"` (`authority-guard.sh`, `pr-draft-guard.sh`, `unattended-guard.sh`); this harness does **not** honor `.*` as a catch-all — only exact/pipe-delimited matchers (`"Bash"`) fire. Additionally, MCP tools are **absent from PreToolUse's documented supported-tools list** (Bash/Write/Edit/Read/Glob/Grep/WebFetch/WebSearch/Agent/AskUserQuestion/ExitPlanMode only), so **no** PreToolUse hook can gate an `mcp__*` call regardless of matcher. Net: the draft-PR ceiling (`pr-draft-guard`), the interpreter-write/symlink residual (`authority-guard`), and — critically — the entire `unattended-guard` mechanical layer are DARK here. Evidence: (a) a `python3 -c "print('render.yaml')"` probe matching both of authority-guard's regexes RAN instead of denying; (b) `mcp__github__update_pull_request draft:false` un-drafts succeeded this session (pr-draft-guard would block if firing); (c) `review-gate.sh`/`push-guard.sh` (`"Bash"` matcher) DO fire | **P1** | open (diagnosed 2026-07-16) | **Still enforced (attended posture holds):** the settings `deny` RULES (authority-path Edits/Writes, `enable_pr_auto_merge`, `.env`) — permission-system-enforced, they blocked `.claude/**` edits this session; the two `"Bash"`-matcher hooks; the auto-mode classifier. **Dark:** draft ceiling, interpreter-write residual, and unattended-guard IF Routines run in this same harness (the 7b gating input). **Fix:** (1) replace every `".*"` matcher with **exact per-tool matcher ENTRIES** (one each for `"Bash"`, `"Edit"`, `"Write"`, `"MultiEdit"`, `"NotebookEdit"`) — only exact single-name matchers are confirmed to fire here; the pipe-alternation form (`Edit|Write|...|Bash`) is UNPROVEN (live-tested 2026-07-16, inconclusive because a running container can't hot-reload settings — needs a fresh session on the branch with the change already committed). No-regression: `.*` already does nothing, so worst case stays inert. See `proposed-settings-v4.json` on `claude/hook-matcher-fix-2026-07-16`; (2) MCP protections can't be hooked → move to permission `deny` rules (`mcp__server__tool`, and try param-deny `(draft:false)`); `enable_pr_auto_merge` already is; (3) **7b unattended write authority MUST NOT rely on these hooks** — needs a dedicated unattended settings profile that `deny`-rules the write surface + the real backstops (branch protection [plan-gated per R5], RO-role re-point [0039 applied 2026-07-16, MCP re-point pending]). **⚠️ CORRECTION 2026-08-14 (`SB0-02`) — what changed, and what did not.** (a) **Fix (1) landed.** `.claude/settings.json` no longer contains a single `".*"` matcher: `PreToolUse` now carries five exact per-tool matcher entries — `"Bash"` (review-gate, push-guard, authority-guard, pr-draft-guard, unattended-guard), `"Edit"`, `"Write"`, `"MultiEdit"`, `"NotebookEdit"` (authority-guard + unattended-guard on each). Landed via PRs #51 (`d90e986`, v4 settings) and #53 (`a56670f`, bash-wrapped invocation); re-verify with `grep -n matcher .claude/settings.json`. (b) **Observed firing, 2026-08-14:** `authority-guard.sh` denied two Bash commands in this session for referencing an authority path alongside a write-capable utility — the `"Bash"`-matcher path is demonstrably live. (c) **NOT verified and therefore NOT claimed:** that the `"Edit"`/`"Write"`/`"MultiEdit"`/`"NotebookEdit"` hook entries fire in *this* harness. Authority-path Edits are separately blocked by the settings `deny` rules, which is a different mechanism — do not read a successful deny as proof the hook fired. (d) **Still open, unchanged:** MCP tools remain outside PreToolUse's supported-tool list, so no hook can gate an `mcp__*` call — item (2) stands; and item (3) stands in full — **7b unattended write authority must still not rely on these hooks.** (e) **The "branch protection [plan-gated per R5]" clause in item (3) is SUPERSEDED** — protection has been live since 2026-07-17 (see R5's own 2026-08-12/08-13 corrections), but with `required_approving_review_count: 0` and `enforce_admins: false` it binds the ref, not the session, and CODEOWNERS remains **advisory**. **Nothing in this correction closes R16, downgrades its severity, or grants any tier.** | `docs/proposals/hook-matcher-fix-2026-07-16.md`; probe + `claude-code-guide` research 2026-07-16; cross-ref R5; `SB0-02` 2026-08-14 re-verification against `.claude/settings.json` |
| R17 | **A second AI-coding runtime (Cursor Cloud Agents) operates on this repo outside every mechanical control this register assumes** — discovered 2026-08-19 via a pasted Cursor session transcript, not a probe of this harness. Verified against that transcript's own live-tested findings: (a) `.claude/settings.json`'s `allow`/`deny` permission arrays are Claude-Code-CLI-only and inert for Cursor Cloud Agents; (b) the `tools:` allowlist in every `.claude/agents/*.md` frontmatter (the thing making `arbi`/`arbi-red-team`/the finance conformance agents/investment-analysis agents "read-only, advisory") is **not enforced by Cursor** — a Cursor-invoked subagent inherits full parent tool access unless `readonly: true` is set, and none of ours have it; (c) `.claude/hooks/authority-guard.sh` fires via a Claude-Code-compatibility shim in Cursor, but the payload Cursor sends omits `cwd`, so it failed closed on every Write call regardless of path in that session — a bug, not a protection, and it drove that session to build via shell workaround instead; (d) a live `arbi-red-team` dispatch from that session fabricated a citation (claimed two docs said "Status: RATIFIED by James" when they say "draft, owner to ratify: James") — real evidence the adversarial gate can hallucinate under load; no `arbi-evals.md` golden case covers it (G1–G7 exist, no G8) | **P1** | open (diagnosed 2026-08-19, unmitigated) | Distinct from R2 (DB role scoping only) and R16 (this harness's own hook-matcher gaps) — this is the entire `tools:`/hook enforcement fence being absent for a *different* tool operating on the same repo. Draft-PR ceiling held in the observed session (nothing merged), so blast radius so far is bounded, but nothing mechanical enforces that ceiling from the Cursor side. Next step: decide whether Cursor Cloud Agents are brought inside the authority fence (native `.cursor/hooks.json` port + `readonly: true` on every advisory agent + a genuinely scoped read-only DB connection for that runtime) or excluded from operating on this repo at all — a governor call, not something arbi can self-authorize | pasted Cursor session transcript, 2026-08-19; corroborates PR #140's independent "review identity ≠ PR author" finding |

## How arbi uses it

- **Surface the open P0/P1 rows every brief** — R1 (Model A) leads BLOCKERS until closed.
- **Close with a note, don't delete.** When a risk clears (e.g. rule #11 lifts → R1 closed),
  set status `closed <date>` and record how; keep the row for audit.
- **New risks land here first**, then get referenced from `roadmap-state.md` — this is the
  canonical risk list.
