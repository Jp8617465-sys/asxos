---
status: candidate
completed: true
dream_date: 2026-07-21
window: 2026-07-15 .. 2026-07-21 (second consolidation; baseline = approved-lessons.md L1–L16)
branch_note: >
  arbi-dream-policy.md's conventional branch is claude/arbi-mem/2026-07-21; this run uses the
  session's harness-assigned branch claude/investment-selection-results-pz9wnc instead — the
  same precedent the 2026-07-15 dream set (both are claude/**, dream-candidates/ only, draft
  PR, never main, never a merge; the branch NAME is a convention, not a safety boundary).
  Note the PR carrying this candidate also carries this session's other doc commits (wake
  snapshot, proposals, close artifacts) — same mixed-PR shape as #46.
inputs:
  # captured by commit SHA for provenance
  - ef1d473  docs/session-handoff-2026-07-21.md      # newest handoff (also 07-18/17/16 read)
  - ef1d473  docs/product/decision-log.md             # rows 2026-07-15 .. 2026-07-21
  - ef1d473  docs/product/arbi-run-ledger.md          # rows full-auto-2026-07-15 .. close-2026-07-21
  - d90e986  docs/product/risk-register.md
  - ef1d473  docs/product/roadmap-state.md
  - 2184ea2  docs/product/memory/approved-lessons.md  # baseline L1–L16 (L8–L16 folded via #48, 2026-07-16)
  # memory/working/: NO new notes since the 2026-07-15 dream (newest is 2026-07-14) — this
  # dream consolidates committed ledgers only.
generated_by: /arbi-dream (main loop consolidation; James-directed, attended)
---

# arbi dream candidate — 2026-07-21

**This is a candidate, not memory.** Ladder level 7 — it never overrides a repo doc (4), live
state (3), a ledger row (5), or `approved-lessons.md` (6). Nothing here is authoritative until
`/arbi-promote` lands it via a reviewed merge. arbi never self-approves.

The week's signal clusters into two families: **live-state-vs-recorded-state divergence**
(the dominant, thrice-repeated pattern) and **provenance discipline** (a genuinely new family
opened by the 2026-07-21 session's red-team catch).

---

## Durable lessons (candidates)

### L-cand-21.1 — Live state outranks recorded state: probe before repeating any "X is live/green/current" claim
**Evidence:** ledger `full-auto-2026-07-15` (the 7a Routine `trig_01PiLVYg…` was absent from
the live trigger list while `roadmap-state.md` + `arbi-autonomy-loop.md` both claimed it
wired — the daily brief had silently stopped); handoff-2026-07-21 §Open items (DB migration
count = 94 vs the recorded/`REQUIRED_MIGRATIONS` 93, no matching file — still unresolved);
CLAUDE.md's "16 collection errors" sandbox list vs observed 39/65 (07-16 wake) and 43/72
(07-21 wake). Three independent instances in one week of a doc asserting state the live
system contradicted. **Extends L12** (a framing is a hypothesis) from *claims in docs* to
*operational state specifically*: any "is live / is applied / is green" sentence must be
re-derived from a probe (`list_triggers`, a DB count, a test run) at the moment it's relied
on — its presence in a state doc is a hypothesis, not a fact.
**proposed_target:** `approved-lessons.md`

### L-cand-21.2 — Provenance is part of an artifact's identity: bypass-produced output never shares review footing with pipeline-produced output
**Evidence:** ledger `close-2026-07-21` (red-team CHALLENGE): `agent_runs` #6/#7 were
hand-logged via a direct MCP write after the macro-economist subagent failed on its dead
frontmatter — mechanically valid on every post-hoc check, yet correctly HELD and disclosed
rather than reviewed alongside pipeline-clean #3/#4. The check a validated pipeline ran
*at write time* cannot be fully reconstructed after the fact (the consuming step,
`macro_theses/service.py:124-129`, explicitly trusts log-time validation it never repeats).
A "zero-cost review batch" that flattens unequal provenance is a category error.
**proposed_target:** `approved-lessons.md`

### L-cand-21.3 — Fix the pipe before consuming its bypass output
**Evidence:** decision-log 2026-07-21 row. Ranking "review the workaround's products" above
"repoint the broken frontmatter that forced the workaround" normalizes the bypass as the
recurring pattern — every future discovery-agent invocation hits the identical break until
Step 0 lands. Standing rule adopted in the same row: **no further hand-logged `agent_runs`
rows before the frontmatter repoint.** Companion to L-cand-21.2: that one governs how to
*review* bypass output; this one governs when to *stop producing* it.
**proposed_target:** `approved-lessons.md`

### L-cand-21.4 — Proposals cheap, enactment gated: a deny that removes the *drafting* channel is a governance bug, not a hardening
**Evidence:** ledger `secperf-pack-corrections-2026-07-16` — the always-on GitHub-MCP
write-tool deny severed attended agents' only draft-via-PR channel for authority files;
James removed it himself (`f4822b5`). The governance model wants proposing to be cheap and
*enactment* to be gated; a control that makes both expensive confuses "can act" with "can
propose."
**proposed_target:** `approved-lessons.md`

### L-cand-21.5 — A governor override of infra gates is not an override of newly-discovered defects
**Evidence:** ledger `secperf-loop-2026-07-15` — James ordered the write loop live (posture
3, overriding the 7b infra gates), but the mandated adversarial pass then surfaced new HIGH
findings postdating his verdict; the loop was correctly NOT fired and the decision returned
to him with the new facts. "Perfect it" means fix what the perfection pass finds — an
override authorizes proceeding past *known, accepted* risks, never past defects discovered
after the authorization.
**proposed_target:** `approved-lessons.md`

### L-cand-21.6 — Verify a resource-limit error's actual scope before treating it as a hard cap
**Evidence:** ledger `close-2026-07-18` — the 6-lens audit's attempt 1 died on what an agent
reported as a "monthly spend limit"; it was a *session* limit that reset, and the re-run
completed all 6 lenses with 0 errors. Being too conservative about a misdiagnosed limit
nearly left a James-requested audit incomplete.
**proposed_target:** `approved-lessons.md`

### L-cand-21.7 — (technique) Git-worktree isolation for fixes that must not disturb concurrent work
**Evidence:** ledger `close-2026-07-18` — #59's test-break fix was verified in an isolated
worktree so the concurrently-running audit's tree stayed clean. Reusable operating
technique, not a behavioural lesson.
**proposed_target:** `project-facts.md`

## Repeated mistakes (≥2 → durable pattern)

1. **Recorded-state drift (3× this window):** dead 7a Routine vs "WIRED" docs; 93-vs-94
   migration count; the twice-rotted sandbox test-gap list. → L-cand-21.1.
2. **Hand-replication standing in for the real code path (recurred despite L7/L11):**
   Phase 2a's SQL-ordering incident (already L7/L11) recurred in a new costume as the
   #6/#7 hand-log. The family lesson (L7/L11) held for *verification*; the new failure mode
   was *production* of artifacts outside the pipeline. → L-cand-21.2/21.3 close that flank.

## Stale conclusions to retire (superseded — listed, not silently deleted)

1. **"The promote half of the dream loop is untested (0 promotions ever)"** (dream-2026-07-15
   frontmatter, ledger `dream-2026-07-15`) — superseded 2026-07-16: James's own merge of #48
   folded L8–L16 into `approved-lessons.md`. Both halves of the loop are now proven.
2. **"supabase-ro not connected / RO MCP never granted"** (ledger `dev-mode-2026-07-16`
   blocked-note) — superseded same-day: `supabase-ro` verified live, connecting as
   `supabase_read_only_user`; migrations 0038+0039 applied (count 93 at the time).
3. **"macro_theses = 0 rows / governed_active_macro_theses empty"** (every doc through
   2026-07-21 morning) — superseded by the first approvals (macro_theses #6/#7, this close).

## Contradictions for James (surfaced, never auto-resolved)

1. **`roadmap-state.md` State-header "Decisions needed from James" still lists HUBS
   concentration + VGS/VAS holding-lot data** — but `james-inbox.md` marks both RESOLVED
   (2026-07-13 / 2026-07-16). The header row rotted while the inbox stayed current. Fix is a
   one-line doc edit; flagged rather than made because the header is wake-owned state.
2. **DB migration count 94 vs `REQUIRED_MIGRATIONS=93`** (`api/main.py:15`) with 39 files on
   disk — either a stray prod-side apply or a stale constant; the startup gate only checks
   `count < REQUIRED`, so a *surplus* passes silently. Needs the diagnostic query next wake.
3. **CLAUDE.md "Known test environment gaps" (16 errors)** vs 43 failed/72 errors observed
   this session — the list's own self-warning ("prone to rotting") has now fired twice;
   consider replacing the enumerated list with the re-derivation command alone.

## Safety carried verbatim (never summarised)

- **Rule #11 (CLAUDE.md):** do not use Model A output — signals, candidate scans, allocator
  runs, or new thesis proposals derived from it — as a basis for real capital decisions.
  Standing policy for v1_5; lifts only when a NEW model version passes a pre-registered
  decay bar (positive, monotonic conviction→21d return) AND earns `approved_for_allocation`.
- **s766B personal-advice firewall:** the system never places, stages, or implies an order;
  `ASXOS_PERSONAL_USE=1` gates every portfolio/tax surface; arbi automates *what gets
  built*, never *what to trade*.
- **Permission tiers:** irreversible tiers (5–7) stay `always_ask`/disabled regardless of
  track record; I5/I6 actions only ever per-action James-authorized; arbi never edits its
  own constitution/boundaries.

## Dedup log

- `memory/working/`: no notes newer than 2026-07-14 — nothing to dedupe (all prior notes
  consolidated by the 2026-07-15 dream; this dream reads committed ledgers only).
- Cross-ledger: `secperf-pack-2026-07-15`'s "verify security regex against a runnable
  harness" is NOT re-proposed as a new lesson — it is the existing L11 applied correctly
  (the system *following* a lesson is evidence the lesson works, not a new lesson).
- `dev-mode-2026-07-16`'s "red-team CHALLENGE was the right call (spec-first caught a
  duplicate build)" and `close-2026-07-21`'s CHALLENGE are merged as joint evidence under
  L-cand-21.2/21.3 rather than a separate "trust the red-team" lesson — two instances of the
  gate working as designed.
