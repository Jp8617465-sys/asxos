# James's inbox — the decisions only the governor can make

**Status:** current · living queue
**Scope:** the single place for decisions reserved to James (governor), not arbi (controller) —
capital, merge approval, migration approval, policy/conviction, broker execution. arbi surfaces
these; it never decides them.
**Last verified:** 2026-08-24 (Amendment G recorded — not a full inbox sweep. `auto` in
user settings is ⚙️ execution-pending. GitHub App is ⚙️ execution-pending. Auto-merge stays
denied. Dark-launch #1/#4 still decide by **2026-08-28**. `0045` still unapplied.)
_Prior: 2026-08-22 (`/arbi-run` + `#154` merge — **the migration `0044` row is
CLOSED**: applied 2026-08-21 as `20260821080458`, live count 97, Patch 0 confirmed present in the
column comment; deadline met. `REQUIRED_MIGRATIONS` 96→97 residual discharged on `main` via
`#154` with the ahead-drift test. The **dark-launch surface #2 row is also closed** — arbi issued
the fresh SHIP verdict it was waiting on. Still open: `0045` (unapplied, *not* time-boxed),
scheduling `detect_theme_stages`, the agent-role **repoint** (not a REVOKE on the inert
`asxos_agent_ro`), and the two 2026-08-31 dark-launch expiries — decide by **2026-08-28**.)
_Prior: 2026-08-20 (three rows appended after the post-merge live-data validation. Eight rows
total. The 2026-08-17 dark-launch rows and the three 2026-07 rows are unchanged and remain as the
audit record.)_
**Docs-truth correction:** 2026-08-22 (a postscript was appended to the resolved `0044` row: the
`REQUIRED_MIGRATIONS` 96→97 residual is discharged; the `0044`/`0043` migration-header residual
stands and is James's. Row text otherwise unchanged; annotation only, no new ruling.)
**Docs-truth correction:** 2026-08-20 (a postscript was appended to the resolved Cursor-ratification
row after PRs #144/#142/#141 merged — its "Model A has not been deleted" clause is now reversed,
and its merge caveat is only partly discharged with #140 still open. Row text otherwise unchanged;
annotation only, no new ruling, so `Last verified` is unchanged.)
**Glyph key:** ✅ resolved · ⚙️ ruled, execution pending · ⏳ open · 🔴 open + time-boxed or
capital-adjacent
**Owner:** arbi appends open items (I2, command-invoked) and marks them resolved once James rules;
James is the only one who closes a row by deciding. Append-only — never delete a resolved row.
**Superseded by:** N/A

Everything else arbi can sequence, draft, or dispatch inside its permission ladder
(`arbi-permission-model.md`). The five classes below are the ones that **only James** can settle,
because they commit real capital, cross an irreversible or governed boundary, or encode a value
judgement arbi has no standing to make:

| Class | Why it's James's | Examples |
|---|---|---|
| **Capital** | arbi never touches real money (firewall + rule #11). | position sizing, deploy/hold, cash floor changes |
| **Merge approval** | CODEOWNER review is the poisoning firewall; arbi never self-approves (`arbi-promotion-gate.md`). | any PR to `main`, dream promotions |
| **Migration approval** | schema is canonical + irreversible; applied via Supabase MCP, not by arbi. | new `migrations/*.sql`, backfills |
| **Policy / conviction** | value judgements (risk appetite, conviction numbers) are governor inputs, not derivable. | `conviction_level`, risk tolerance, quarantine calls |
| **Broker execution** | s766B firewall — the system never places or implies an order. | every real trade, ESPP disposal timing |

---

## Open items (arbi is blocked or best-effort-degraded until James rules)

| Item | Why it's James's | What arbi needs to proceed |
|---|---|---|
| ✅ **RESOLVED 2026-07-13 — HUBS concentration policy** (was: reframed 2026-07-12) | Policy/conviction. HUBS is **ESPP/employer stock** (James works at HubSpot); ruled as a **single-employer concentration ceiling**, not a 1–5 conviction (a rating is a category error for comp he didn't choose — `conviction_level` correctly stays NULL for HUBS). | **DECISION (James, 2026-07-13): soft-flag at 10% of investable capital, hard trim-trigger at 20%.** Employer-stock ceiling (matches the per-name cap; income↔wealth correlation argues for a tight leash). HUBS is currently **locked/unsellable**, so act by diluting with new capital and trim toward 10% when the window opens. **Enforcement DEFERRED to the roadmap** ("build later when it matters" — James): the ESPP marker + `employer_concentration_cap_pct` + the 10/20 flag fold into the `security_kind` enum build (`m14_candidate_security_kind_enum` / new `m14_candidate_espp_employer_concentration`). Live position at decision: 24 sh ≈ 7,175 AUD, **+2.8% AUD** (not the false −29%), **100% of portfolio** (sole holding, locked). Basis: portfolio-coherence-reviewer analysis 2026-07-13. |
| ⚙️ **RULED 2026-07-16 — CBA thesis #1: automate stale-thesis hygiene** (was: fix or retire) | James, 2026-07-16: "unsure why cba thesis is still a thing — it should be automated." Ruling read: a 4×-price-detached `watching` thesis should never sit in his inbox waiting for manual numbers — the discipline system should detect price-detachment and auto-move the row to a terminal/review state. | **Two actions:** (1) build the automation — a deterministic `price_detached` discipline check (entry/stop/target vs live price beyond a sanity band → auto-flag, and for `watching` rows auto-propose retire after N days unanswered); model-independent, evidence-only, same lane as `discipline.py`. Backlogged as the next discipline-evaluator increment. (2) The CBA #1 row itself: retire is the implied direction (not held, `watching`, no capital); executing the status write awaits a one-word confirm or lands as the automation's first live action. |
| ✅ **RESOLVED 2026-07-16 — VGS.AU / VAS.AU: NOT HELD; ETF Slice 2 proceeds without James data** (was: holding-lot data needed) | James, 2026-07-16: he does **not** hold VGS/VAS — they were named to exercise how non-equity instruments (ETFs) are treated end-to-end, not as real positions. The prior framing ("real positions not yet recorded") was wrong. | Nothing from James. ETF Slice 2 reframes to **clearly-marked demo/paper lots** (paper-trade lane or a flagged demo fixture — never `holding_lots`, which stays real-capital-only) exercising the full non-equity path: `security_kind='etf'`, kind-aware ingestion (Slice 2a, merged), ETF screening criteria (proposal 2026-07-12), CGT/distribution treatment. Unblocked — buildable now. |
| ⏳ **OPEN — dark surface #1, portfolio brief: expires 2026-08-31, needs a fresh verdict.** Decide by **2026-08-28**. | Capital-adjacent + policy. `dark-launch-exit-plan.md` gives the flip owner as **James**: the gate is `ASXOS_PORTFOLIO_BRIEF_ENABLED=1` + `ASXOS_PERSONAL_USE=1`, and the surface's own re-scope condition is "model-independent cards + 4wk sign-off". arbi cannot flip a personal-advice-firewall gate. | One of **SHIP / DELETE / KEEP-DARK** with a new expiry. Per `dark-launch-exit-plan.md:152-159` an expired KEEP-DARK re-raises automatically and must earn a fresh verdict, not silently roll over. If KEEP-DARK: name the new date and what must be true by then. **Process note: this row is 5 vets overdue** — the expiry was surfaced by four consecutive `arbi-red-team` vets (2026-08-13 → 2026-08-17) and carried forward each time without being written here, which is the defect the file's own rule exists to prevent. |
| ⏳ **OPEN — dark surface #4, paper-trade evaluator: expires 2026-08-31, needs a fresh verdict.** Decide by **2026-08-28**. | Capital-adjacent. Flip owner **James**; the gate is "start the 4wk run", which `dark-launch-exit-plan.md:133` records as James's because it commits to a capital-adjacent evaluation. Re-raises together with surface #1. | One of **SHIP (start the 4wk run) / DELETE / KEEP-DARK** with a new expiry. Note the coupling: surface #1's own re-scope condition includes a 4wk sign-off, so a KEEP-DARK here likely forces a KEEP-DARK there — rule them together rather than separately. |
| ✅ **RESOLVED 2026-08-19 — Cursor-session product rulings ratified** (was: OPEN, needs formal ratification) | Policy/conviction + architecture direction. A parallel Cursor Cloud Agent session (2026-08-18, outside this repo's governed loop) recorded three verbal rulings from James — `detect_theme_stages` **KEEP**, `monitor_paper_portfolio` **DROP**, `build_portfolio` **DELETED** (replaced by a proposed segment-valuation → selection → exposure architecture, draft PR #142). | **DECISION (James, 2026-08-19): ratified verbatim** ("ratifying cursor sessions") — recorded as `roadmap-state.md` Amendment F. The three cron rulings and the `build_portfolio`→segment-valuation redirect (PR #142's direction) are now this repo's actual decision. **Not ratified by this:** merge/ready of PRs #140/#141/#142 (still James-only; PR #141 was independently found ~60% complete by a same-session Claude review — see Amendment F for the gap list) or PR #142's five cited data-substrate defects as fixed (findings to act on, not yet actioned). Rule #11 untouched; Model A remains **not deleted** from this repo (correction stands). `risk-register.md` R17 (the separate Cursor-runtime governance/security gap) is unaffected by this ratification and stays open. **Postscript 2026-08-20:** the caveats above are **partly** resolved. James merged #144 (Model A now **is** deleted, reversing the "not deleted" correction in this row; rule #11 and its generic gate are genuinely untouched and still stand), #142 (D1/D2/D3 built as real code — **D4/D5 remain deferred**, so "five defects fixed" is still not true) and #141 (its ~60% gap list closed pre-merge). **#140 is still open**, so the merge caveat naming #140/#141/#142 is not fully discharged. `detect_theme_stages` KEEP is still **not executed** — no Actions home. Text above unchanged as the audit record of the 08-19 ruling. |
| ✅ **RESOLVED 2026-08-21 — `0044` is applied; this row's deadline is discharged** (was: 🔴 OPEN, apply before Sat 2026-08-22 16:00 UTC). Verified at the primary source, not from a claim: `supabase_migrations.schema_migrations` = **97**, `rs_fundamentals_pit.currency` **present**, latest version `20260821080458`. Applied by a sibling session on 2026-08-21 (branch `claude/asx-stock-evaluation-p0hxx2`), which also corrected `0044`'s `COMMENT ON COLUMN` — the repo file's "102 symbols" figure was wrong by ~9x against a live measurement of 947, and that string becomes permanent DB metadata. **Residuals, both benign:** `main`'s `REQUIRED_MIGRATIONS = 96` still lags the observed 97 (the bump sits on that unmerged branch; the check only hard-fails when the DB has *fewer* than required, so nothing is broken), and the repo copy of `0044` still reads "DRAFT - NOT APPLIED" and no longer matches the database. `0045_segment_map.sql` was **not** applied and remains pending. **Process note:** this row stayed 🔴 for the rest of the day after the work was done, and on 2026-08-21 *two independent advisors* read it and both escalated the migration as the single most time-critical item in the repo — inheriting urgency from a stale row. A doc asserting a state production had already left, exactly the failure class the same session was fixing elsewhere. **Postscript 2026-08-22 (annotation only — no new ruling, row stays RESOLVED):** the first residual is discharged — `REQUIRED_MIGRATIONS` is bumped **96 → 97** (`asxos/api/main.py:14`), re-measured against the live ledger (97, latest `20260821080458`), and the asymmetry that made the lag benign is now pinned by a test (`tests/test_api_main.py::test_migration_drift_passes_above_required`, which fails under a `!=` mutation). The second residual **stands and is James's**: the repo copy of `0044` still reads `DRAFT — NOT APPLIED` and its `COMMENT ON COLUMN` still carries the superseded `102 symbols` figure that was corrected at apply time, so file and database now disagree; `0043`'s header still claims "PRODUCTION-READY — still unapplied". Both are `migrations/**`, Edit-denied to agents. `0045` is still genuinely unapplied — its DRAFT header is correct and must not be swept up with the other two. | Migration approval. PR #142 merged code that UPSERTs `rs_fundamentals_pit.currency`; **verified 2026-08-20 that the column does not exist in production**. `weekly-research.yml` runs `derive_fundamentals_pit.py` as an *ordered blocking step*, so the run fails there and also blocks `sync_fundamentals` below it. | **First apply Patch 0** in `segval-live-validation-2026-08-20.md` — `0044`'s comments carry two superseded figures, one of which (`:29`, "102 symbols" vs the measured **947**) lands in `COMMENT ON COLUMN` as permanent DB metadata. Comment text only; the DDL is correct. Then apply `0044` via Supabase MCP, re-run `SELECT count(*) FROM supabase_migrations.schema_migrations` and bump `REQUIRED_MIGRATIONS` (currently 96, matching applied) to the **observed** count — not a guessed +1, per `api-conventions.md`. `0045_segment_map.sql` is the same batch but not time-boxed: nothing runs `build_segment_map` yet (it has no workflow home either). **arbi's defect** — code and migration were drafted together but only code could merge. |

| ⚙️ **RULED 2026-08-24 — `defaultMode: auto` in `~/.claude/settings.json`, then a week of log data before any hook `allow` rewrite** (Amendment G ruling 1). | Policy / harness. User-level setting; an agent must not apply it. The 2026-07-14 deny-only decision on `push-guard.sh` / `pr-draft-guard.sh` stays until the log is read ~2026-08-31. | James applies the runbook `docs/product/runbooks/claude-code-user-settings.md`. Re-read `.claude/permission-requests.log` plus noticed-prompt count. ASK rows are not prompt count (friction proposal §1.1). Cursor Cloud does not write this log (R17). |
| ⚙️ **RULED 2026-08-24 — GitHub App reviewer, not a second account** (Amendment G ruling 2). | Merge-gate identity. Browser + secrets; agent must not create the App or paste the private key. | Follow `docs/product/runbooks/arbi-approver-github-app.md`. `pull-requests: write` only — no `contents: write`. Do not flip `required_approving_review_count` or `enforce_admins` until the throwaway-branch test passes. CODEOWNERS on James-authored authority PRs still deadlocks. Second-account runbook is the rejected alternative. |
| ✅ **RESOLVED 2026-08-24 — no auto-merge; keep the click** (Amendment G ruling 3). | Merge approval. Rejected-list item 9 widened from docs-only to any path. No CFR/MTTR baseline; auto-merge is earned after the check suite is trustworthy. | Nothing from arbi. `enable_pr_auto_merge` stays denied. |
| ⏳ **OPEN — schedule `detect_theme_stages`; confirm the KEEP ruling is in force** (`segval-live-validation-2026-08-20.md`, Patch 1). | **Two things, and the first is genuinely James's.** (a) The KEEP ruling is recorded as `roadmap-state.md` Amendment F — which is **not on `main`**; it lives on unmerged PR #143, and `scheduler-inventory-2026-08-13.md:196` still classifies this job **DECIDE / governor call**. So the ruling is not yet in force in-repo. (b) Once it is, the edit is a tool boundary, not a decision. | Confirm KEEP (or merge #143, which records it), then either paste Patch 1 into `.github/workflows/daily-brief.yml` **or** — the more normal shape here — let arbi land it as a draft PR for your merge approval, since `claude-execute` may edit workflow files on a `claude/**` branch. Placement is load-bearing: the brief's `theme_dashboard` collector reads `themes.stage_suggested`, so running it after the send makes every brief show yesterday's suggestion. Upstream it needs only `Sync prices`. Needs no new secret. Theme stages un-refreshed since at latest 2026-08-12. Also stale once applied and not Edit-able by arbi: `job-conventions.md`'s "12 steps" enumeration, `scheduler-inventory-2026-08-13.md:196`, `target-architecture.md:1678`. |
| ✅ **RESOLVED 2026-08-23 — PR #157 merged** (`2679602`). **Postscript 2026-08-24:** live GitHub wins; do not re-ask. The review-gate `touch` allow inside that audit is obsolete (#158 deleted the gate). | Merge approval — discharged by merge. | Nothing further on this row. |
| ✅ **RESOLVED 2026-08-21 via PR #149 — `/pm-review` no longer reads Model A; ONE VERIFICATION STILL OWED** (was: 🔴 OPEN, treat `/pm-review` as unsafe). **This row's own scope was wrong, and that is the finding worth keeping.** Patch 2 named `thesis-coherence-guard` alone; a pre-ship adversarial security review found a **second unamputated reader still in the fan-out** — `portfolio-coherence-reviewer` read `signals` in four places, including a "Signal vs holding alignment" section emitting `Model A = SELL since [date], 34 days ago`. With `as_of` frozen that counter **grows forever**, rendering an ever-more-urgent-looking stale SELL into the synthesis that produces EXIT-CANDIDATE verdicts — worse than the defect this row named. Shipping to spec would have closed the leak in the report, not the product. Both agents amputated (model-independent checks survive in each); `/pm-review` five→four; the worked example that taught the synthesizer to cite `Model A BUY 0.68` replaced; `CLAUDE.md:234` no longer advertises "ML SHAP evidence" as a sanctioned category and the `signals` schema entry is marked FROZEN at the point of lookup. Verified: **zero `FROM signals` query paths remain** across `.claude/agents/`. **OWED:** the completion artifact — a `/pm-review HUBS.NYSE` run showing four agents and zero Model A figures — is not yet observed, and could not be produced in the authoring session (agent definitions load at session start, so that session still held the pre-amputation versions; running it there would have reproduced the leak). A fresh session owes it. **Honest limit:** every control here is prompt-level — `mcp__supabase-ro__execute_sql` permits any SELECT, so the frozen table stays reachable and *not reading it* is the control. `REVOKE SELECT ON signals` for the agent role (`m14_candidate_agent_db_role_scoping`) is the only mechanical control that would survive a prompt edit. | Capital-adjacent + rule-#11-adjacent (a policy call on a capital-facing evidence path), and `.claude/agents/`, `.claude/commands/` and `CLAUDE.md` are all Edit-denied to this session. | The agent's **step 1** query is `FROM signals WHERE model='model_a'`; PR #144 deleted every writer, so it returns **frozen Model A SHAP evidence presented as current** into the `/pm-review` synthesis that informs holding decisions — worse than an error, because it looks like a working answer. **Amputate rather than retire:** its step 4 (`thesis_revisions` JOIN `theses`, thesis-fatigue detection) is live and model-independent, and deleting the whole agent throws that away. Note the containment gap: its frontmatter says "use **PROACTIVELY**", so avoiding `/pm-review` does not cover auto-invocation — the warning belongs in the agent file. Full edit surface listed in Patch 2. |

**Postscript 2026-08-22:** the `/pm-review HUBS.NYSE` verification owed by the #149 row was observed. Four fan-out agents, **zero** Model A / `signals` figures, verdict **REVIEW**. Direct `thesis-coherence-guard` on thesis 2 returned **NEEDS REVIEW** (revisit due 2026-08-03, 19d overdue). The mechanical residual is unchanged: `REVOKE SELECT ON signals` for the agent role is still James's.

---

## How arbi uses it

- **Surface open rows every wake** in the brief's "Decisions needed from James" line — these are
  the items that gate real progress and that arbi *cannot* self-serve, distinct from the ranked
  next-action queue arbi can drive itself.
- **Never act as if a row is resolved** until James rules. A blocked capital/policy item stays
  blocked; arbi degrades best-effort around it (e.g. skips the conviction check) rather than
  guessing.
- **Close by deciding, not by deleting.** When James rules, mark the row resolved with the date
  and the decision, and cross-reference where it landed (`decision-log.md` /
  `portfolio-outcome-ledger.md` / the migration). Keep the row for audit.
