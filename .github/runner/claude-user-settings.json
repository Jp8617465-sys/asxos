{
  "permissions": { "defaultMode": "auto" },
  "autoMode": {
    "environment": [
      "$defaults",
      "Owner: James, sole user and repo admin. Primary use: arbi, an autonomous chief-of-staff agent maintaining github.com/Jp8617465-sys/asxos, a single-user investment tool.",
      "Source control: github.com/Jp8617465-sys/asxos. Production is GitHub Actions workflows running from main; merge is deploy.",
      "Database: the asxos Supabase project, via the supabase and supabase-ro MCP servers.",
      "Key internal services: Resend (email), Healthchecks.io (deadman pings)."
    ],
    "allow": [
      "$defaults",
      "Merging a pull request in Jp8617465-sys/asxos with squash once required checks have passed on its current head, including a pull request the agent opened itself. The owner has delegated merge authority to the agent in AGENTS.md.",
      "Applying a migration to the asxos Supabase project after the backup.yml workflow has concluded success in the same session.",
      "Dispatching GitHub Actions workflows in Jp8617465-sys/asxos, including backup.yml.",
      "Editing files under .claude/ and .github/workflows/ in Jp8617465-sys/asxos as part of the requested task."
    ]
  }
}
